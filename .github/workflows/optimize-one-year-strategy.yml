name: optimize-one-year-strategy

on:
  push:
    branches: [main]
    paths: ['.github/workflows/optimize-one-year-strategy.yml']
  workflow_dispatch:

concurrency:
  group: optimize-one-year-strategy
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by precision-btc-eth-optimization"
