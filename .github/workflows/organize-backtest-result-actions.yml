name: Archived backtest UI organizer

# The UI migration this one-shot workflow performed is already present in the
# app. Keep a harmless manual record instead of an invalid push-time workflow.
on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Migration status
        run: echo "Backtest result UI organization is already applied."
